import React from 'react';
import styles from './BaseButton.module.scss';

const BaseButton: React.FC = () => {
    return (
        <button className={styles.BaseButton}></button>
    )
}

export default BaseButton;